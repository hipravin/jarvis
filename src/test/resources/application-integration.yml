spring:
  config:
    import: ${SECRETS_PROPERTIES_PATH:file:./secrets-integration.properties}

bookstore:
  loader-root-path: "src/test/resources/data/bookstore"
  initialload:
    enabled: false


logging:
  level:
    ru.hipravin: DEBUG
    hipravin: DEBUG
    org.springframework: INFO
    org.springframework.test.context.cache: DEBUG
#    org.hibernate.engine.jdbc.spi.SqlExceptionHelper: OFF
#    org.hibernate.SQL: DEBUG
#    org.postgresql: TRACE #log exact queries, even bytea content. Not suitable for prod because of verbosity
